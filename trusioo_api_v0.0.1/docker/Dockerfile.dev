# 开发环境Dockerfile（带Air热重载）
FROM golang:1.21-alpine

# 设置工作目录
WORKDIR /app

# 安装必要的包
RUN apk add --no-cache git ca-certificates tzdata

# 安装Air用于热重载（使用兼容Go 1.21的版本）
RUN go install github.com/cosmtrek/air@v1.40.4

# 复制go mod文件
COPY go.mod go.sum ./

# 下载依赖
RUN go mod download

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 8080

# 使用Air启动应用程序
CMD ["air", "-c", ".air.toml"]